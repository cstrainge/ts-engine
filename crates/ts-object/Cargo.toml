
[package]
name = "ts-object"
description = "Implementation of the system's basic object model and protocols."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[features]

[dependencies]
ts-object-macros = { path = "../ts-object-macros" }
ts-process = { path = "../ts-process" }
async-trait = "0.1"
